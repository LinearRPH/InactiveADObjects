README

This project finds inactive users and computers (aka objects) given a proper LDAP path and a timespan.

It also finds objects that have been disabled and not modified (hence neglected). This is helpful for clean up.